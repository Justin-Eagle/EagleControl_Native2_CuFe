<Window x:Class="EagleControl_Native2_CuFe.UnLockView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EagleControl_Native2_CuFe"
        mc:Ignorable="d"
        Title="UnLockView" Height="215" Width="500" WindowStartupLocation="CenterScreen">
    <Border CornerRadius="5"
        BorderBrush="#7F7F7F"
        BorderThickness="2"
        Background="#3D7878"
        Padding="20"
        Width="500"
        HorizontalAlignment="Center"
        VerticalAlignment="Center" Height="200">
        <StackPanel Margin="13,0,13,-14">
            <TextBlock x:Name="Title" FontSize="25" FontWeight="Bold"  Margin="0,0,0,20" Width="350" TextAlignment="Center"><Run Text=""/></TextBlock>

            <Grid>
                <!-- 密碼輸入框 -->
                <PasswordBox x:Name="InputPasswordBox"
                     FontSize="25"
                     BorderBrush="Gray"
                     BorderThickness="1"
                     Height="50"
                     Margin="0"
                     HorizontalContentAlignment="Center"
                     VerticalContentAlignment="Center"
                     Visibility="Visible"/>

                <!-- 顯示明文 -->
                <TextBox x:Name="RevealPasswordBox"
                 FontSize="25"
                 BorderBrush="Gray"
                 BorderThickness="1"
                 Height="50"
                 Margin="0"
                 HorizontalContentAlignment="Center"
                 VerticalContentAlignment="Center"
                 Visibility="Collapsed"
                 IsReadOnly="True"/>

                <!-- 顯示/隱藏切換按鈕 -->
                <Button Content="👁"
                Width="30"
                Height="30"
                VerticalAlignment="Center"
                HorizontalAlignment="Right"
                Margin="0,0,5,0"
                Click="TogglePasswordVisibility_Click"
                Background="Transparent"
                BorderThickness="0"/>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button Content="取消" Width="70" Height="36" Margin="0,0,10,0" Background="#A0A0A0" Foreground="White" FontSize="15" FontWeight="Bold" Click="Cancel_Click"/>
                <Button Content="確認" Width="70" Height="34" Background="#3CB371" Foreground="White" FontSize="15" FontWeight="Bold" Click="Confirm_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
